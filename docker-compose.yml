services:
  postgres:
    image: postgres:16-alpine
    container_name: scout-postgres-container
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 88228701ta
      POSTGRES_DB: scout-postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
